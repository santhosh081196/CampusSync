# Privacy

Calendar Canvas Dashboard is designed as a local desktop application.

- Google Calendar access is read-only.
- Canvas data is requested directly from the Canvas instance configured by the user.
- The application does not include analytics, advertising, or an external application server.
- Settings and tokens remain on the user's computer in Electron's application-data directory.

Users should revoke the Canvas token and Google OAuth access when they no longer use the application.
